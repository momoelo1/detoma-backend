node_modules

# credenziali locali: mai in git (il template è .env.example)
.env
.env.*
!.env.example

# richieste .rest di prova: possono contenere credenziali reali
requests/

# foto sorgente degli import una tantum: pesano ~8MB e servono solo in
# locale mentre si carica il catalogo, non al server
scripts/import-assets/


.vercel
.env*
